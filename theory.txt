pwd : shows the route you are
ls : shows the files 
git init : inicitialize a new proyect
git status : to see the state of our files.
git add : to add a file to your workplace.
git commit : to  create a starting point of our code.
git config --global user.email "email@gmail.com" : To log in the user's email
git config --global user.name "name" : To identify the userd.
Esc + : + wq : To log out from vim after the commit.
git log : to see all the commits we make.
git checkout -- filename.extention : to revert the changes on the files
git diff filename.extension : To see the differences between the files.



